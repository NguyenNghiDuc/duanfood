{
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.10",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "express-validator": "^7.0.1",
    "multer": "^2.2.0",
    "mysql2": "^3.22.5",
    "sqlite3": "^5.1.6"
  },
  "name": "baitap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "node -e \"const { execSync } = require('child_process'); try { const pids = execSync('lsof -ti tcp:5000', { encoding: 'utf8' }).split(/\\s+/).filter(Boolean); if (pids.length) execSync('kill -9 ' + pids.join(' '), { stdio: 'ignore' }); } catch (e) {}\"",
    "dev": "nodemon app.js",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
